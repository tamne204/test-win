[pytest]
pythonpath = . apps/capcut-v2
testpaths = tests
python_files = test_*.py
markers =
    fast: Fast unit, component, and snapshot tests (< 2s)
    integration: Offline pipeline integration and draft normalization tests
    accuracy: Full accuracy benchmark and adversarial tests
    release: Release gate and packaging validation tests
